(dirs :standard \ node_modules)

(melange.emit
 (target output)
 (alias app)
 (libraries reason-react create_melange_app bindings)
 (modules :standard)
 (preprocess
  (pps {{#each ppxs}}{{this}}{{#unless @last}} {{/unless}}{{/each}}))
 (module_systems es6))
