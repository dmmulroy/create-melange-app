{
  "name": "{{name}}",
  "version": "0.0.1",
  "description": "Enter a description of your app here",
  "scripts": {
    {{#each scripts}}
    "{{@key}}": "{{this}}",
    {{/each}}
  },
  "dependencies": {
    {{#each dependencies}}
    "{{@key}}": "{{this}}",
    {{/each}}
  },
  "devDependencies": {
    {{#each devDependencies}}
    "{{@key}}": "{{this}}",
    {{/each}}
  }
}
